<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="avatarUrl"
            type="String" />

        <variable
            name="title"
            type="String" />

        <variable
            name="onMainClickListener"
            type="com.wkz.pleasedreading.main.OnMainClickListener" />
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/pr_fl_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true">

        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/pr_statusBarSize"
            android:background="@color/pr_statusBar" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/pr_actionBarSize"
            android:layout_marginTop="@dimen/pr_statusBarSize"
            android:background="@color/pr_actionBar"
            android:paddingStart="15dp"
            android:paddingEnd="15dp">

            <com.wkz.framework.widgets.FRInsLoadingView
                android:id="@+id/pr_ilv_avatar"
                android:layout_width="@dimen/pr_actionBarSize"
                android:layout_height="@dimen/pr_actionBarSize"
                android:layout_centerVertical="true"
                android:onClick="@{onMainClickListener::openDrawer}"
                android:src="@drawable/pr_shape_placeholder_avatar_circle"
                app:avatarLoadingUrl="@{avatarUrl}"
                app:fr_ilv_end_color="@color/fr_color_light_purple"
                app:fr_ilv_start_color="@color/fr_color_pink"
                app:fr_ilv_status="loading" />

            <android.support.v7.widget.AppCompatTextView
                android:id="@+id/pr_tv_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginStart="15dp"
                android:layout_marginEnd="15dp"
                android:gravity="center"
                android:text="@{title}"
                android:textColor="@color/fr_color_black"
                android:textSize="24sp" />
        </RelativeLayout>
    </FrameLayout>
</layout>